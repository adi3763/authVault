# authVault
 AuthVault — a Node.js/Express + MongoDB authentication backend with bcrypt password hashing,     ▎ JWT access tokens, rotating refresh tokens with reuse detection, secure logout, and an           ▎ email-based password reset flow using one-time expiring tokens.
